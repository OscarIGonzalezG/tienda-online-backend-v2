package com.tiendaonline.backend.entity;

/**
 * Enum que representa los roles que un usuario puede tener.
 * Se guarda como texto en la DB.
 */
public enum Role {
    ADMIN,
    VENDEDOR,
    CLIENTE
}
